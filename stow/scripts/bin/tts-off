#!/usr/bin/env bash
# Disable TTS notifications for Claude
set -euo pipefail

STATE_FILE="${XDG_STATE_HOME:-$HOME/.local/state}/claude-tts-enabled"
rm -f "$STATE_FILE"
echo "TTS notifications disabled"
