# treehouse Configuration
# Pooled, pre-warmed git worktrees for parallel coding agents
# https://github.com/kunchenguid/treehouse

if command -v treehouse >/dev/null 2>&1; then
    alias th=treehouse
fi
