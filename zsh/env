if [[ $SHLVL < 3 ]]; then
  mydir=`dirname $0`

  for f in $mydir/??_*; do
    source $f
  done
fi

export AUTOFEATURE=true
export RSPEC=true
export EDITOR=mate
export GEM_HOME=/usr/local/lib/ruby/gems/1.8